interface:
  display_name: "Switch Instruction Files"
  short_description: "切换并记住项目指令文件来源，用于跨目录复用指令"
  default_prompt: "Use $switch-instruction-files to switch the active AGENTS.md and related instruction files."
policy:
  allow_implicit_invocation: true
